# UI Improvement Plan

## Goal
Elevate the application's design to a modern, premium aesthetic using vanilla CSS and Bootstrap overrides.

## Changes

### 1. Global Styles (`styles.css`)
-   [MODIFY] Define a modern color palette (Inter typography, softer shadows).
-   [MODIFY] Reset default Bootstrap behaviors for a cleaner look.
-   [MODIFY] Add global animations for route transitions or hover effects.

### 2. Navigation Bar (`app.html`, `app.css`)
-   [MODIFY] Move from default "bg-light" to a custom gradient or solid premium color.
-   [MODIFY] Improve link typography and hover states.

### 3. Data Tables (`orders.html`, `customers.html`, `products.html`)
-   [MODIFY] Style tables with modern borders, hover rows, and better spacing.
-   [MODIFY] Add badges for status or IDs.

### 4. Order Details (`order-details.html`, `order-details.css`)
-   [MODIFY] Convert standard Cards to "Glassmorphism" or soft-shadow cards.
-   [MODIFY] Improve typography for labels and values.

## Verification
-   Visual check via browser (manual).
